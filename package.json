{
  "name": "claude-token-counter",
  "version": "3.0.0",
  "description": "Extensión MV3 que cuenta tokens en claude.ai y notifica por Telegram",
  "private": true,
  "scripts": {
    "test": "node --test browser/test/*.test.js",
    "lint": "eslint browser/"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
